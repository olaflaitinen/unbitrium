name: Benchmark

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  benchmark:
    name: Run Standard Benchmarks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"

      - name: Run benchmarks
        run: |
          # Placeholder for benchmark run command
          python -c "print('Running standard benchmarks...')"
          # In real usage: python -m unbitrium.bench.run --config benchmarks/standard.yaml
